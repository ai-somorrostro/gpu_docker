# =============================================================================
# HYPERPARAMETER TUNING WITH RAY TUNE
# =============================================================================
# Ejecuta directamente (sin renombrar):
# ./train.sh train_yolo_with_ray_tune.py datasets/coco8/coco8 coco8.yaml env.raytune.example

# =============================================================================
# BASIC TRAINING PARAMETERS
# =============================================================================

EPOCHS=80
BATCH_SIZE=64
IMG_SIZE=640
PATIENCE=20

# Model configuration
MODEL=yolov8s.pt

# Límite de VRAM (0.0-1.0, donde 1.0 = 100%)
VRAM_FRACTION=0.90

# =============================================================================
# RAY TUNE CONFIGURATION
# =============================================================================

# Number of hyperparameter search iterations
TUNE_ITERATIONS=30

# Grace period for ASHA early stopping (epochs before trials can be stopped)
TUNE_GRACE_PERIOD=10

# Metric to optimize ('metrics/mAP50(B)' for mAP@0.5)
TUNE_METRIC=metrics/mAP50(B)

# =============================================================================
# RAY TUNE HYPERPARAMETER SEARCH RANGES
# =============================================================================
# These define the search space for hyperparameter optimization.
# Format: PARAM_MIN and PARAM_MAX for each tunable parameter.
# Ray Tune will use tune.uniform(MIN, MAX) for each parameter.

# Learning rate search range
LR0_MIN=0.00001
LR0_MAX=0.01

# Final learning rate factor range
LRF_MIN=0.01
LRF_MAX=0.5

# HSV-S augmentation range
HSV_S_MIN=0.0
HSV_S_MAX=0.9

# HSV-V augmentation range
HSV_V_MIN=0.0
HSV_V_MAX=0.9

# Rotation degrees range
DEGREES_MIN=0.0
DEGREES_MAX=45.0

# Translation range
TRANSLATE_MIN=0.0
TRANSLATE_MAX=0.3

# Scale range
SCALE_MIN=0.0
SCALE_MAX=0.9

# Shear range
SHEAR_MIN=0.0
SHEAR_MAX=10.0

# Mixup augmentation range
MIXUP_MIN=0.0
MIXUP_MAX=1.0

# =============================================================================
# FIXED AUGMENTATION PARAMETERS (no se optimizan)
# =============================================================================

# Fixed values - no tunable
PERSPECTIVE=0.0
FLIPUD=0.0
FLIPLR=0.5
HSV_H=0.015
MOSAIC=1.0
CLOSE_MOSAIC=10
